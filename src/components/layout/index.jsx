import React from "react";

const Layout = ({children}) => {
    return (
        <section>
            <header>
                <header>
                    header
                </header>
                {children}
            </header>
        </section>
    )
}

export default Layout;